# 💡 أفكار والمزايا المستقبلية (Future Suggestions)

يحتوي هذا الملف على الأفكار المعمارية والمزايا الوظيفية التي تم اقتراحها لتطوير منصة "ميزان" مستقبلاً، ليتم الرجوع إليها عند التخطيط لمراحل التطوير القادمة.

## 🏛️ المزايا الجوهرية (Core Features)

1. **نظام إدارة القضايا (Cases Module - `/cases`):**
   - السماح بإنشاء ملفات قضايا شاملة برقم الدعوى والمحكمة.
   - ربط القضية بأكثر من محامي من الفريق.
   - تتبع حالات القضية (منظورة، محجوزة للحكم، منتهية).
   - توفير مساحة لرفع وتخزين المستندات عبر `Supabase Storage`.

2. **الأجندة والجلسات (Sessions & Calendar - `/sessions`):**
   - ربط كل جلسة بقضية معينة.
   - عرض الجلسات في مكتبة تقويم (Calendar) متقدمة (مثل FullCalendar).
   - إرسال إشعارات (أو إيميلات عبر Resend) لتذكير المحامي المكلف بموعد الجلسة في اليوم التالي.

3. **لوحة المهام الإدارية (Kanban Task Board - `/tasks`):**
   - توفير لوحة سحب وإفلات (Drag & Drop) باستخدام `@dnd-kit`.
   - توزيع المهام على الإداريين والسكرتاريا داخل المكتب (مثل: استلام مستند، دفع رسوم، الاتصال بموكل).
   - تتبع الإنجاز (To Do, In Progress, Done).

4. **سجل الموكلين (Client Directory - `/clients`):**
   - بناء قاعدة بيانات لعملاء المكتب (أفراد وشركات).
   - ربط الموكل بكافة قضاياه ومستحقاته المالية، لتسهيل استخراج كشف حساب أو تقرير حالة لعميل معين.

## ✨ تحسينات تجربة المستخدم (UX Improvements)
- **نظام الفواتير المالي (Billing):** إصدار فواتير ذكية للموكلين وتتبع الدفعات وتصديرها بصيغة PDF.
- **تطبيق الجوال (Mobile App):** تحويل لوحة تحكم المحامين لتطبيق جوال تنبيهي باستخدام (PWA) للإشعارات الفورية.
